# Anchor token rotation — 2026-08-01

The fine-grained PAT used by api/cron-tree-head.js to push signed tree heads to this repo was rotated on 2026-08-01 (prior token was expiring 2026-08-04). New token expires 2027-08-01 (365-day maximum). The Ed25519 STH signing key is unchanged — existing and future tree heads verify exactly as before.
